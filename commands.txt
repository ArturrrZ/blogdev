---------------PROD MODE with Docker------------------------
stripe login ..
stripe listen --forward-to localhost:80/api/webhooks/stripe/
- get the api key and put into .env
docker compose -f docker-compose.prod.yml up --build
