---------------DEV MODE with Docker------------------------
stripe login ..
stripe listen --forward-to localhost:8000/api/webhooks/stripe/
- get the api key and put into .env
docker compose -f docker-compose.dev.yml up --build
